{
  "name": "create",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=testing jest __tests__",
    "build": "ncc build index.js --minify",
    "watch": "ncc build index.js --watch --minify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "node-fetch": "^2.6.1",
    "require-directory": "^2.1.1",
    "yaml": "^1.10.0"
  },
  "devDependencies": {
    "@atlassian-partner-engineering/eslint-config": "^3.1.1",
    "@zeit/ncc": "^0.22.3",
    "eslint": "^7.12.1",
    "jest": "^26.6.3",
    "nock": "^13.0.4"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "/helpers/"
    ]
  }
}
